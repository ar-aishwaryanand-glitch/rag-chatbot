# ===== MINIMAL STARTER (Python 3.14 Compatible) =====
# Basic utilities - install first
python-dotenv>=1.0.0

# LLM Providers
groq>=0.4.0  # Groq API for fast LLM inference
langchain-groq>=0.1.0  # LangChain integration for Groq
google-genai>=1.0.0  # Optional: for Google embeddings

# Embeddings
langchain-huggingface>=0.0.1  # HuggingFace embeddings integration
sentence-transformers>=2.2.0  # Local embeddings models

# Core dependencies with relaxed constraints
langchain-core>=0.1.0
langchain-google-genai>=0.1.0  # Optional: for Google embeddings

# Simplified LangChain (without heavy dependencies)
langchain-community>=0.0.1
langchain-text-splitters>=0.0.1

# Vector Store Options
# chromadb>=0.4.0  # Requires pybind11 + compilation on Python 3.14
faiss-cpu>=1.7.0  # Local vector store, no compilation needed

# Pinecone (Optional - for production cloud vector database)
pinecone-client>=3.0.0  # Pinecone vector database client
langchain-pinecone>=0.1.0  # LangChain integration for Pinecone

# UI Framework
streamlit>=1.30.0  # Web-based UI for RAG system

# Agent Framework
langgraph>=0.0.20  # Agent orchestration with state graphs
langgraph-checkpoint>=1.0.2  # Checkpointing for agents
langgraph-checkpoint-postgres>=1.0.0  # PostgreSQL checkpoint storage

# Agent Tools
duckduckgo-search>=3.8.0  # Free web search
numexpr>=2.8.0  # Safe math evaluation for calculator
RestrictedPython>=6.0  # Sandboxed code execution

# Web Agent Tool (Phase 4)
playwright>=1.40.0  # Browser automation for web agent
beautifulsoup4>=4.12.0  # HTML parsing
readability-lxml>=0.8.1  # Article content extraction
lxml>=4.9.0  # XML/HTML processing (required by readability)

# Document Upload Support (Phase 4+)
pypdf>=3.17.0  # PDF file parsing
python-docx>=1.0.0  # DOCX file parsing

# PostgreSQL Database (Optional - for persistent session storage)
psycopg2-binary>=2.9.0  # PostgreSQL adapter for session storage (psycopg2)
psycopg[binary]>=3.1.0  # PostgreSQL adapter for checkpoint storage (psycopg3)

# Policy Engine (Optional - for agent behavior control)
pyyaml>=6.0  # YAML configuration for policies

# Redis Message Queue (Optional - for distributed task processing)
redis>=5.0.0  # Redis client for message queue

# OpenTelemetry (Optional - for observability and monitoring)
opentelemetry-api>=1.20.0  # OpenTelemetry API
opentelemetry-sdk>=1.20.0  # OpenTelemetry SDK
opentelemetry-exporter-otlp>=1.20.0  # OTLP exporter for Jaeger, Honeycomb, etc.
opentelemetry-exporter-jaeger>=1.20.0  # Jaeger exporter (optional)

# ===== INSTALL INSTRUCTIONS =====
# If you get build errors, try:
# pip install --upgrade pip setuptools wheel
# pip install --no-build-isolation -r requirements.txt
#
# Or switch to Python 3.11/3.12:
# brew install python@3.12
# python3.12 -m venv venv && source venv/bin/activate

# ===== ADD THESE LATER (Heavy packages) =====
# chromadb>=0.4.0  # Vector database
# sentence-transformers>=2.2.0  # ~2GB download (PyTorch + models)
# fastapi>=0.109.0
# uvicorn>=0.27.0
# pypdf>=3.17.0
# gradio>=4.0.0
